apiVersion: v2
name: pleme-gaveta
description: gaveta — typed coordination service for the gaveta self-hosted sync drive (Garage-backed)
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: pleme-lib
    version: ">=0.28.0"
    repository: "file://../pleme-lib"
