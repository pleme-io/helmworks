{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "pleme-lib.fullname" . }}
  namespace: {{ include "pleme-lib.namespace" . }}
  labels:
    {{- include "pleme-lib.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "pleme-lib.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Intra-cluster RPC (Garage peers reach each other on the headless svc)
    - from:
        - podSelector:
            matchLabels:
              {{- include "pleme-lib.selectorLabels" . | nindent 14 }}
      ports:
        - protocol: TCP
          port: 3901
    # Prometheus scrape on admin port
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - protocol: TCP
          port: 3903
    {{- with .Values.networkPolicy.extraIngress }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  egress:
    # DNS
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # Intra-cluster RPC
    - to:
        - podSelector:
            matchLabels:
              {{- include "pleme-lib.selectorLabels" . | nindent 14 }}
      ports:
        - protocol: TCP
          port: 3901
{{- end }}
