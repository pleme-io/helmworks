apiVersion: v2
name: pleme-garage
description: Garage S3-compatible object store — geo-distributed, Rust, AGPL.
type: application
version: 0.1.0
appVersion: "v2.1.0"

dependencies:
  - name: pleme-lib
    version: "~0.5.0"
    repository: "file://../pleme-lib"
