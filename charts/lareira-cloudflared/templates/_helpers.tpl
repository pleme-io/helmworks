{{/*
Common labels delegated to pleme-lib for fleet-wide consistency.
fullname helper is also from pleme-lib so it picks up fullnameOverride
+ nameOverride + Release.Name semantics in one place.
*/}}
