apiVersion: v2
name: pleme-zot
description: Cluster-local Zot OCI registry — air-gap registry-mirror role. Deploys a Zot StatefulSet that is the cluster's only path to external image bytes (via image-sync) or, in pull-mode, the only workload allowed external egress to a curated upstream allowlist.
type: application
version: 0.1.1
appVersion: "2.1.5"

dependencies:
  - name: pleme-lib
    version: "~0.13.0"
    repository: "file://../pleme-lib"
