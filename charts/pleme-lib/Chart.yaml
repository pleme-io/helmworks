apiVersion: v2
name: pleme-lib
description: Shared library chart for pleme-io internal services
type: library
version: 0.45.0
# 0.44.0 (2026-08-07) -- scaleToZero.v1: scale-to-zero as a declared QUALITY.
#
# templates/_scaletozero.tpl. `pleme-lib.scaleToZero.v1` +
# `.replicasField` + `.num`. A workload declares the wake CONTRACT it needs and
# gets the right scaler, or the render fails. Additive: _breathability.tpl is
# untouched and all seven of its consumers are unaffected.
#
# WHY A VERSIONED TEMPLATE NAME, and why it must never be renamed. Helm's
# named-template namespace is global and flat, so `pleme-lib.<name>` resolves to
# whichever vendored copy is parsed last. Measured in this repo on 2026-08-07:
# 116 packaged pleme-lib copies at 17 distinct versions, and 96 of the 108
# in-chart copies still carry the pre-fix `replicas | default 1`. An older copy
# that lacks a define renders NOTHING, silently, exit 0. `.v1` cannot be
# shadowed by a copy that does not define it; if every copy in the graph is old
# the include fails outright. Silent-nothing becomes a loud failure. Add `.v2`
# beside it; never rename this one.
#
# WHAT IT REFUSES, each a failure already lived somewhere in the fleet:
#   * zero wake arms, or two -- a scaler that renders nothing while the
#     workload reports healthy is the whole failure class this exists to close
#   * a `wake.kind` contradicting the arm actually present
#   * `autoscaling.enabled` alongside scaleToZero on the same workload --
#     _deployment.tpl gates spec.replicas on a DIFFERENT values tree, so both
#     means Helm and KEDA fight over the field every reconcile (pleme-sui and
#     pleme-nix-builder are both in that state today under the old template)
#
# TWO THINGS _breathability.tpl CANNOT DO, which is why this is a rewrite and
# not a rename:
#   * it emits no annotations block, so a ScaledObject rendered from it can
#     never carry autoscaling.keda.sh/paused-replicas -- park is an operator
#     floor that must WIN over any autoscaler, so a partial it cannot reach is
#     unusable fleet-wide
#   * pleme-lib.labels hardcodes app.kubernetes.io/part-of: nexus-platform.
#     Appending an override emits a DUPLICATE KEY that strict YAML rejects --
#     proven by this template's own suite before the labels became a real
#     map merge rather than appended text.
#
# Zeros survive. `default 4 $b.max` rewrites max: 0 to 4 and `default 300
# $b.cooldown` rewrites cooldown: 0 to 300 -- both still live in
# _breathability.tpl. `.num` is a `kindIs "invalid"` nil-check, the idiom
# _deployment.tpl already uses, and the suite asserts all four bounds in both
# directions.
#
# THE SEAL. `rest.assert.enabled` renders a PrometheusRule with three rules
# against the metric a kube-state-metrics custom-resource-state collector
# exports (`keda_scaledobject_state_info`, labels
# name/exported_namespace/ready/active):
#   ScalerRejected        -- KEDA reports a rejected ScaledObject ONLY in its
#                            own status. On 2026-08-04 every ScaledObject in
#                            one namespace was rejected for a same-value cron
#                            pair, no HPA was ever created, and nothing else
#                            said so.
#   DidNotReturnToRest    -- replicas above the floor while the trigger reports
#                            inactive, for cooldown + slack. The orphaned-worker
#                            shape: work finished, capacity never released.
#   NeverObservedAtRest   -- cheapest and strongest. A workload that has never
#                            been seen at its floor is broken or mis-declared.
# A StatefulSet target asserts against kube_statefulset_replicas, because a
# rule querying the Deployment metric for a StatefulSet can never fire, and a
# seal that cannot fail is decoration.
#
# 21/21 green. Six are RED RUNS on the refusals, and five cover the seal --
# those five exist because the seal shipped BROKEN and the suite passed 16/16
# anyway: no test enabled it, so a whole rendered object went unexercised. A
# `{{- /* ... */ -}}` inside the rules list ate the trailing newline and merged
# `rules:` into `- alert:`. Verified by reintroducing that exact trim and
# watching four assertions fail.
#
# 0.43.0 (2026-08-05) -- trust: the private-CA anchor as a chart-side quality.
#
# templates/_trust.tpl. A chart declares `trust.enabled` + a domain and gets its
# serving Certificate from the domain's CA issuer plus the anchor propagated to
# its process, instead of hand-rolling both. Issued leaves carry isCA
# explicitly: zot's original hand-applied cert had NO basicConstraints, so it
# could never be a trust anchor no matter what any node was told, and it was
# pinned into node userData anyway where it did nothing for weeks.
#
# Refuses `trust.selfSigned` outright -- a SelfSigned issuer mints a new key per
# install, invalidating every pinned copy of the anchor at the next cluster
# inception, which is the defect being removed. Does NOT set SSL_CERT_FILE by
# default: it REPLACES the OpenSSL bundle rather than adding to it, so a bare
# private CA there breaks every public TLS call the workload makes.
#
# The fingerprint is recorded as an annotation, not checked -- Go templates have
# no digest function. `ancora verify` (pleme-io/ancora) reads it and does the
# cross-surface comparison.
#
# ⚠️ THE 0.42.0 NOTE BELOW IS STALE AND IS CORRECTED HERE. It said the last
# published pleme-lib on ghcr was 0.40.1 and that 0.40.2/0.41.0/0.42.0 were git
# -only. Re-measured 2026-08-05 against the registry: 0.42.0 IS published. The
# publish pipeline was fixed at some point after that note was written and the
# note was never updated -- the same coverage-claim rot it warns about
# elsewhere. Verify with:
#   gh api /orgs/pleme-io/packages/container/charts%2Fpleme-lib/versions
#
# 0.42.0 (2026-07-26) -- breatheBand: the RequestBand (RESERVATION) arm.
#
# Every band arm before this one carves a LIMIT. A limit bounds blast radius,
# but it is not the field that decides survival: `oom_score_adj` and the QoS
# class are derived from the REQUEST, and a BestEffort pod (no requests AND no
# limits) is unbandable by every other dimension. Receipt: sui-cache-pg took 34
# OOMKills at a 202.8Mi high-water against a 1Gi limit with cgroup failcnt=0 —
# the limit was never binding, and no MemoryBand setting at any value could
# have saved it.
#
# Three deliberate departures from the arms above, each a safety property:
#   * defaults OFF, and when on defaults to `mode: shadow`;
#   * emits NO `dryRun` key at all — breathe retired it (inert for 8 of 10 band
#     kinds), and shipping an inert field that LOOKS like a safety gate is worse
#     than shipping none;
#   * `resource` is REQUIRED with no default — guessing between the OOM lever
#     (memory) and the scheduling lever (cpu) is the ambiguity this dimension
#     exists to remove.
#
# `manifestRef: {path, marker}` is the durable half: the in-place resize mutates
# the POD, not the template, so without a committed manifest the reservation —
# and with it the QoS posture — evaporates on the next rollout.
#
# NOT DELIVERED BY THIS COMMIT. The last pleme-lib published to
# ghcr.io/pleme-io/charts is 0.40.1 (verified against the registry tag list);
# 0.40.2, 0.41.0 and this 0.42.0 are all in git and NOT on ghcr. Nothing here
# reaches a cluster until the publish pipeline is fixed.
#
# 0.41.0 (2026-07-26) -- breatheBand: honor `postureRef`.
#
# The template previously emitted the full tuning tuple (setpoint /
# growAbove / growFactor / shrinkBelow / shrinkFactor / cooldownSeconds /
# disruptionPolicy / maxStalenessSeconds) UNCONDITIONALLY from its own
# hardcoded defaults -- which happen to be the retired `platform-default`
# numbers. breathe resolves each field `inline.or(posture).or(default)` PER
# FIELD, so a `postureRef` set alongside those inline keys resolved LAST and
# did nothing. Every Helm-emitted band in the fleet was therefore pinned to
# the retired tuple with NO WAY to opt into the criticality tiers, while
# raw-YAML bands moved to postures freely -- a whole class of workload
# (including a database and the fleet control plane) invisible to any
# file-based posture audit, because there was no file to audit.
#
# A tuning field is now omitted exactly when a posture is named AND values
# did not set it explicitly, so the posture actually binds. Rendering is
# byte-equivalent (modulo field order) when no postureRef is set -- verified
# by a normalized before/after diff across pleme-microservice.
#
# See the template header for why `hasKey` and not `default`: the
# never-shrink postures use `shrinkBelow: 0`, which a `default` filter would
# silently rewrite to 0.7.
annotations:
  org.opencontainers.image.source: https://github.com/pleme-io/helmworks
