apiVersion: v2
name: pleme-ocis
description: OwnCloud Infinite Scale — single-binary Go Drive backed by S3.
type: application
version: "0.2.0"
appVersion: "7.3.2"

dependencies:
  - name: pleme-lib
    version: "~0.15.0"
    repository: "file://../pleme-lib"
