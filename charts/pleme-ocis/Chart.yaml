apiVersion: v2
name: pleme-ocis
description: OwnCloud Infinite Scale — single-binary Go Drive backed by S3.
type: application
version: 0.1.1
appVersion: "7.4.0"

dependencies:
  - name: pleme-lib
    version: "~0.10.0"
    repository: "file://../pleme-lib"
