apiVersion: v2
name: pangea-operator
description: Pangea infrastructure operator — manages InfrastructureTemplate CRDs with OpenTofu backend
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: pleme-lib
    version: "~0.4.0"
    repository: "file://../pleme-lib"
