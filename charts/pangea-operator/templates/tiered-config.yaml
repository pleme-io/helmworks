{{/*
Tiered progressive-discovery config — the file tier of the operator's shikumi
OperatorConfig, rendered as a reconciled ConfigMap (enjulho: config-as-Helm).

DESTINATION FORM (tier-honest): default-off (`config.file.enabled: false`), so
the LIVE operator render is byte-unchanged. Renders the ConfigMap only when an
operator opts in — the committed target for the config.rs file-tier discovery
follow-on (the env → reconciled-Helm cutover). Secrets are NEVER rendered here
(see the mixin header + values.yaml `config:` doc).
*/}}
{{- include "pleme-lib.tieredConfig.configMap" . }}
