apiVersion: v1
kind: Service
metadata:
  name: {{ include "pleme-attic.fullname" . }}
  labels:
    {{- include "pleme-attic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "pleme-attic.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
