apiVersion: v2
name: pleme-attic
description: Attic Nix binary cache — ro platform cache layer
type: application
version: 0.2.0
appVersion: "0.1.0"

dependencies:
  - name: pleme-lib
    version: "~0.5.0"
    repository: "file://../pleme-lib"
