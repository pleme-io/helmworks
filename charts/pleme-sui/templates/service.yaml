apiVersion: v1
kind: Service
metadata:
  name: {{ include "pleme-sui.fullname" . }}
  labels:
    {{- include "pleme-sui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "pleme-sui.selectorLabels" . | nindent 4 }}
  {{- with .Values.service.ports }}
  ports:
    {{- toYaml . | nindent 4 }}
  {{- end }}
