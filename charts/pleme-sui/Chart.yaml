apiVersion: v2
name: pleme-sui
description: "sui (粋) — Rust-native Nix build + cache platform (ro platform)"
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: pleme-lib
    version: "~0.5.0"
    repository: "file://../pleme-lib"
