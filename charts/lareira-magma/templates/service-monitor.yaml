{{/* Render the reconciler ServiceMonitor via the pleme-reconciler library. */}}
{{- $ctx := dict
    "Chart"   .Chart
    "Release" .Release
    "Values"  (index .Values "pleme-reconciler" | default dict) -}}
{{- if $ctx.Values -}}
{{- include "pleme-reconciler.serviceMonitor" $ctx }}
{{- end }}
