apiVersion: v2
name: pleme-microservice
description: Application chart for HTTP services (REST/GraphQL/gRPC)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: pleme-lib
    version: "0.3.0"
    repository: "file://../pleme-lib"
