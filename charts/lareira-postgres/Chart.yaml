apiVersion: v2
name: lareira-postgres
description: pleme-lareira home-services chart for a single PostgreSQL instance (DR shared project DB + family services).
type: application
version: 0.1.0
appVersion: "16.6"

dependencies:
  - { name: pleme-lib,     version: "~0.5.0", repository: "file://../pleme-lib" }
  - { name: pleme-lareira, version: "~0.1.0", repository: "file://../pleme-lareira" }
