apiVersion: v2
name: lareira-pandoc-render
description: pleme-lareira chart for the pandoc-render service (markdown → typeset PDF for manuscript previews).
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - { name: pleme-lib,     version: "~0.10.0", repository: "file://../pleme-lib" }
  - { name: pleme-lareira, version: "~0.1.0", repository: "file://../pleme-lareira" }
