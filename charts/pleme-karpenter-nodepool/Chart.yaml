apiVersion: v2
name: pleme-karpenter-nodepool
description: Declare a Karpenter NodePool + EC2NodeClass via typed Helm values — a thin, workload-agnostic wrapper around pleme-lib's _karpenter.tpl helpers
type: application
version: 0.1.0
appVersion: "1.8.1"
dependencies:
  - name: pleme-lib
    version: ">=0.40.1"
    repository: "file://../pleme-lib"
