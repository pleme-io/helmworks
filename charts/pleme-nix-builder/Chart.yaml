apiVersion: v2
name: pleme-nix-builder
description: Nix remote builder — KEDA zero-scale via NATS build queue (ro platform)
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: pleme-lib
    version: "~0.4.0"
    repository: "file://../pleme-lib"
