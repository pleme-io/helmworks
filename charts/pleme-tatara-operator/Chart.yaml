apiVersion: v2
name: pleme-tatara-operator
description: tatara K8s operator — bridges NixBuild CRDs to builders via NATS
type: application
version: 0.2.0
appVersion: "0.1.0"

dependencies:
  - name: pleme-lib
    version: "~0.13.0"
    repository: "file://../pleme-lib"
