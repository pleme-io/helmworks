apiVersion: v1
kind: Service
metadata:
  name: lava-operator
  labels:
    app: lava-operator
spec:
  type: ClusterIP
  selector:
    app: lava-operator
  ports:
  - name: metrics
    port: {{ .Values.metrics.port }}
    targetPort: metrics
