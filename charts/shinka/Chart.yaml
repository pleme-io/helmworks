apiVersion: v2
name: shinka
description: GitOps-native database migration operator — CRD, RBAC, PrometheusRules
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: pleme-lib
    version: "~0.4.0"
    repository: "file://../pleme-lib"
