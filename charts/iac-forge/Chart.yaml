apiVersion: v2
name: iac-forge
description: IaC code generation pipeline with tend watcher and iac-forge sync
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: pleme-lib
    version: "~0.4.0"
    repository: "file://../pleme-lib"
