apiVersion: v2
name: headscale
description: Self-hosted Tailscale coordination server
type: application
version: 0.1.0
appVersion: "0.25.1"

dependencies:
  - name: pleme-lib
    version: "~0.4.0"
    repository: "file://../pleme-lib"
