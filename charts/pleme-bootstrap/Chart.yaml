apiVersion: v2
name: pleme-bootstrap
description: Application chart for one-time bootstrap Jobs (FluxCD-managed, no Helm hooks)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: pleme-lib
    version: "~0.4.0"
    repository: "file://../pleme-lib"
