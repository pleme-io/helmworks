apiVersion: v2
name: pleme-gpu-workload
description: StatefulSet for GPU-accelerated workloads (Ollama, inference servers)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: pleme-lib
    version: "~0.4.0"
    repository: "file://../pleme-lib"
