{{/*
Declare this chart's owned JetStream stream + consumer via the
shared pleme-lib template. The broker (pleme-nats) is unaware of
this stream — Cilium-style identity. Removing the chart removes the
stream.
*/}}
{{- include "pleme-lib.jetstream" . }}
