apiVersion: v2
name: arachne
description: Classifieds scraper with headless Chrome sidecar — browser automation, photo pipeline
type: application
version: 0.2.0
appVersion: "1.0.0"

dependencies:
  - name: pleme-lib
    version: "0.3.1"
    repository: "file://../pleme-lib"
