{{- /*
SUPERSEDED (2026-07-06) — this Tier B stub declared a `sui build-service --reapi`
REAPI *remote-worker* Deployment, a model that was never built (no
`sui build-service --reapi` binary exists) and never proven. The PROVEN camelot
super-cache is the `sui cache serve --backend-config` daemon over the TieredBackend
keystone (Redis L1 → Postgres L2 → S3/local L3) + a tmpfs RAMDISK sandbox — ran
end-to-end on 2026-07-06 (sui/contrib/ground-truth) and shipped as its own
self-contained camelot enjulho artifact:

    helmworks/charts/lareira-sui   (Deployment/sui + Redis + Postgres + RAMDISK
                                    MemoryBand + tiered Secret + example HelmRelease)

This file renders NOTHING (the proven daemon is NOT re-authored here — solve once,
in one place). The builder NODE-fleet concerns (node groups, ARC runners,
ReplicaBand, the RAMDISK MemoryBand, the queue exporter, the BreatheCloudPool)
remain this chart's job; the cache SERVICE is lareira-sui's. Kept as a tombstone
rather than deleted so the supersession is legible in one place.

If a future REAPI remote-worker model is genuinely wanted, extend lareira-sui (the
daemon primitive lives in pleme-sui), never revive this competing surface.
*/ -}}
{{- if false }}
{{- end }}
