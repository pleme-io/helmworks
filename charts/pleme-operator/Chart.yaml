apiVersion: v2
name: pleme-operator
description: Kubernetes operator with RBAC for pleme-io platform
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: pleme-lib
    version: "0.3.0"
    repository: "file://../pleme-lib"
