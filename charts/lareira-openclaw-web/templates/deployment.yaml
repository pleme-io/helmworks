apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openclaw-web.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openclaw-web.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "openclaw-web.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openclaw-web.selectorLabels" . | nindent 8 }}
      annotations:
        # Roll the deployment when runtime config changes.
        checksum/runtime-config: {{ toJson .Values.runtimeConfig | sha256sum }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: openclaw-web
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          readinessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            # /config.js into hanabi's static root (loaded by index.html).
            - name: runtime-config
              mountPath: /app/static/config.js
              subPath: config.js
              readOnly: true
            # Hanabi's own config (server, port, static root, spa fallback).
            - name: hanabi-config
              mountPath: /etc/hanabi/config.yaml
              subPath: config.yaml
              readOnly: true
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: runtime-config
          configMap:
            name: {{ include "openclaw-web.fullname" . }}-runtime
            items:
              - key: config.js
                path: config.js
        - name: hanabi-config
          configMap:
            name: {{ include "openclaw-web.fullname" . }}-hanabi
            items:
              - key: config.yaml
                path: config.yaml
