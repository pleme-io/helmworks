apiVersion: v2
name: hanabi
description: BFF (Backend-for-Frontend) chart — dual port, GraphQL federation, WebSocket proxy, session management
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: pleme-lib
    version: "~0.4.0"
    repository: "file://../pleme-lib"
