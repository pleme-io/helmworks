apiVersion: v2
name: pleme-lib-bare
description: Bare test fixture for exercising pleme-lib compliance validators in isolation. Not intended for installation.
type: application
version: 0.0.0
appVersion: "0.0.0"

dependencies:
  - name: pleme-lib
    version: "~0.10.0"
    repository: "file://../../../charts/pleme-lib"
