suite: pleme-saber service tests
templates:
  - templates/service.yaml
values:
  - ../../examples/saber.yaml
tests:
  - it: should render a Service
    asserts:
      - isKind:
          of: Service

  - it: should be ClusterIP type
    asserts:
      - equal:
          path: spec.type
          value: ClusterIP

  - it: should expose http port 8080
    asserts:
      - contains:
          path: spec.ports
          content:
            name: http
            port: 8080
            targetPort: http
            protocol: TCP

  - it: should expose grpc port 50051
    asserts:
      - contains:
          path: spec.ports
          content:
            name: grpc
            port: 50051
            targetPort: grpc
            protocol: TCP

  - it: should select the correct pods
    asserts:
      - exists:
          path: spec.selector["app.kubernetes.io/name"]
      - exists:
          path: spec.selector["app.kubernetes.io/instance"]
