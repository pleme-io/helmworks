suite: lareira-lacre — security context hardening
templates:
  - charts/pleme-microservice/templates/deployment.yaml
release:
  name: openclaw-gate
  namespace: openclaw
defaults: &defaults
  pleme-microservice.image.tag: "sha256:abcd1234abcd1234abcd1234abcd1234abcd1234abcd1234abcd1234abcd1234"
  pleme-microservice.attestation.signature: "blake3:test-sig-aaaaaaaaaaaaaaaa"
  pleme-microservice.attestation.certificationHash: "blake3:test-cert-bbbbbbbbbbbb"
  pleme-microservice.attestation.complianceHash: "blake3:test-comp-cccccccccccc"
tests:
  - it: pod runs as non-root
    set:
      <<: *defaults
    asserts:
      - equal:
          path: spec.template.spec.securityContext.runAsNonRoot
          value: true

  - it: container drops ALL capabilities
    set:
      <<: *defaults
    asserts:
      - contains:
          path: spec.template.spec.containers[0].securityContext.capabilities.drop
          content: ALL

  - it: container has read-only root filesystem
    set:
      <<: *defaults
    asserts:
      - equal:
          path: spec.template.spec.containers[0].securityContext.readOnlyRootFilesystem
          value: true

  - it: replicaCount >= 2 satisfies CP-2 (HPA disabled path)
    set:
      <<: *defaults
      pleme-microservice.autoscaling.enabled: false
    asserts:
      - equal:
          path: spec.replicas
          value: 3

  - it: container exposes only the proxy port (no debug surface)
    set:
      <<: *defaults
    asserts:
      - equal:
          path: spec.template.spec.containers[0].ports[0].containerPort
          value: 8083
      - equal:
          path: spec.template.spec.containers[0].ports[0].name
          value: http

  - it: REGISTRY env wires lacre to cartorio + backing registry
    set:
      <<: *defaults
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: LACRE_CARTORIO_URL
            value: "http://openclaw-registry-lareira-cartorio-cartorio:8082"
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: LACRE_BACKEND_URL
            value: "http://openclaw-zot:5000"
